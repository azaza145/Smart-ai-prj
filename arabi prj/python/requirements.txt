pandas>=2.0.0
scikit-learn>=1.3.0
mysql-connector-python>=8.0.0
numpy>=1.24.0
scipy>=1.10.0

# PDF: direct text extraction (no OCR) for structured CV parsing
# pip install pymupdf pdfplumber
pymupdf>=1.23.0
pdfplumber>=0.10.0

# Fallbacks if needed
pdfminer.six>=20221105

# Optional OCR for scanned PDFs (requires Tesseract installed on OS)
# pip install pdf2image pytesseract
# OS: apt-get install tesseract-ocr tesseract-ocr-fra
pdf2image>=1.16.0
pytesseract>=0.3.10
